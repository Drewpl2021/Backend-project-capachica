APP_NAME="Laravel"
APP_ENV="production"  # Cambié a 'production' porque estás en un entorno desplegado
APP_KEY="base64:kLuoYkp0IyadvAuhATCz2zX8gVgwRL+USI+6WxRYnUk="
APP_DEBUG="false"  # Cambié a 'false' para producción
APP_URL="http://your-domain.com"  # Cambia esto por tu URL real
LOG_CHANNEL="stack"
LOG_DEPRECATIONS_CHANNEL="null"
LOG_LEVEL="debug"
DB_CONNECTION="mysql"
DB_HOST="your-database-host"  # Reemplaza con la dirección real de tu base de datos
DB_PORT="3306"
DB_DATABASE="TurismoWeb"
DB_USERNAME="root"
DB_PASSWORD="your-database-password"  # Asegúrate de poner la contraseña real
BROADCAST_DRIVER="log"
CACHE_DRIVER="file"
FILESYSTEM_DISK="local"
QUEUE_CONNECTION="sync"
SESSION_DRIVER="file"
SESSION_LIFETIME="120"

# Redis
REDIS_HOST="127.0.0.1"  # Asegúrate de que Redis esté configurado correctamente
REDIS_PASSWORD="null"
REDIS_PORT="6379"

# Mail
MAIL_MAILER="smtp"
MAIL_HOST="mailpit"
MAIL_PORT="1025"
MAIL_USERNAME="null"
MAIL_PASSWORD="null"
MAIL_ENCRYPTION="null"
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS (Deja estos en blanco si no los usas)
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_DEFAULT_REGION="us-east-1"
AWS_BUCKET=""
AWS_USE_PATH_STYLE_ENDPOINT="false"

# Pusher (Deja estos en blanco si no los usas)
PUSHER_APP_ID=""
PUSHER_APP_KEY=""
PUSHER_APP_SECRET=""
PUSHER_HOST=""
PUSHER_PORT="443"
PUSHER_SCHEME="https"
PUSHER_APP_CLUSTER="mt1"

# Variables de Vite
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

PASSPORT_TOKEN_EXPIRATION="250000"
